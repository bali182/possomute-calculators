import type { FC } from "react";

import { PlaceholderPanel } from "../../components/PlaceholderPanel";

export const FramePage: FC = () => {
  return <PlaceholderPanel titleKey="placeholder.frameTitle" bodyKey="placeholder.frameBody" />;
};
