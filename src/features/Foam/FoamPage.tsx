import type { FC } from "react";

import { PlaceholderPanel } from "../../components/PlaceholderPanel";

export const FoamPage: FC = () => {
  return <PlaceholderPanel titleKey="placeholder.foamTitle" bodyKey="placeholder.foamBody" />;
};
